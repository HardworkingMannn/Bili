server:
  port: 8082
spring:
  application:
    name: UserInfoModule
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848

