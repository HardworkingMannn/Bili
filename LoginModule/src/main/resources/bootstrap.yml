server:
  port: 8080
spring:
  application:
    name: LoginModule
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        server-addr: localhost:8848

